pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "🔨 Build aplikasi..."
            }
        }

        stage('Test') {
            steps {
                echo "🧪 Menjalankan test..."
            }
        }

        stage('Deploy') {
            steps {
                echo "🚀 Deploy aplikasi..."
            }
        }
    }
}
